server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SW and manifest must not be cached (updates must propagate immediately)
    location = /sw.js {
        add_header Cache-Control "no-store, no-cache, must-revalidate";
        expires 0;
    }
    location = /manifest.webmanifest {
        add_header Cache-Control "no-store";
        expires 0;
    }

    # Serve static assets with long cache
    location ~* \.(js|css|png|jpg|webp|svg|ico|woff2?)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Proxy API and asset requests to the backend
    location /api/ {
        proxy_pass http://api:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Allow large file uploads (nginx default is 1MB)
        # 200M to support backup restore uploads
        client_max_body_size 200M;

        # Required for SSE streams (state/stream endpoint)
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 3600s;
        chunked_transfer_encoding on;
    }

    location /uploads/ {
        proxy_pass http://api:3001;
        proxy_set_header Host $host;
    }

    # SPA fallback — serve index.html for all unknown paths
    location / {
        try_files $uri $uri/ /index.html;
    }
}
