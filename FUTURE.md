make hotspots position relative to the floorplan image, not the browser window
On the action-button, the icon should have the same options as the state-icon, it should also respond to state on/off events. That way when a user clicks on the button, the icon responds accordingly (e.g. displays the on state when on events, etc.)
